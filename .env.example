# Get a FREE Gemini API key at https://aistudio.google.com (Get API key). No billing required.
# Copy this file to .env and paste your key. .env is gitignored — never commit it.
# NOTE: GEMINI_API_KEY is only required for the `ask` and `ping` commands (LLM generation).
# Indexing and search use local embeddings and do NOT require this key.
GEMINI_API_KEY=

# Optional overrides (defaults shown):
# gemini-flash-latest tracks the current stable Flash model (avoids deprecation churn).
GEMINI_CHAT_MODEL=gemini-flash-latest

# Local embedding model — downloaded once from HuggingFace on first `gdrive-rag index` run.
# The model is cached at ~/.cache/huggingface/ after the initial download.
# Alternatives: BAAI/bge-large-en-v1.5 (larger, better, ~1.3 GB) | all-MiniLM-L6-v2 (tiny, ~90 MB)
LOCAL_EMBED_MODEL=BAAI/bge-base-en-v1.5

# If you are behind an HTTP proxy (e.g. a corporate/dev network), the app needs it to
# reach Google's APIs. Uncomment and set; leave unset on a normal machine.
# https_proxy=http://your-proxy:port
# http_proxy=http://your-proxy:port
